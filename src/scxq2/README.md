# SCXQ2 Lane Definitions

This directory defines the SCXQ2 lane schema and the CM-1 dictionary bytes.

The lane schema is stored with a `.txt` suffix for GitHub compatibility. Rename
`lane.schema.json.txt` to `lane.schema.json` to restore the original extension.
